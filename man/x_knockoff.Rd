% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ghostknockoff.R
\name{x_knockoff}
\alias{x_knockoff}
\title{Generate Ghost Knockoff Samples}
\usage{
x_knockoff(x, monte = 50, seed = 42)
}
\arguments{
\item{x}{A numeric matrix of observations. Rows correspond to samples
and columns correspond to variables.}

\item{monte}{(optional) Number of Monte Carlo knockoff samples to generate.
Default is 50.}

\item{seed}{(optional) Random seed for reproducibility of knockoff generation.}
}
\value{
A numeric array of dimension
\verb{(n_samples, n_variables, monte)} containing knockoff samples.
}
\description{
Generate Ghost Knockoff Samples
}
\details{
The function first estimates the empirical correlation matrix of the input
variables and then applies the Ghost Knockoff construction using the
\code{"asdp"} method. A set of Gaussian perturbations is generated and combined
with the original data through a linear transformation to produce knockoff
variables.

The output is a three-dimensional array where each slice along the third
dimension corresponds to one Monte Carlo knockoff realization.
}
\examples{
set.seed(42)
z = stats::rnorm(100)
x = sin(z) + 0.01 * stats::rnorm(100)
y = sin(x) + exp(z) + 0.01 * stats::rnorm(100)
data = cbind(x, y, z)
kocmi::x_knockoff(data)

}
